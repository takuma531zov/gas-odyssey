# gas-ODYSSEY-master

このリポジトリはGoogle Apps Script（GAS）開発のためのモノレポです。
claspを用いてtypeScriptでローカル開発して、GASにbuild&deployします。


## 構成

- `packages/` - 各GASアプリケーションの開発ディレクトリ

## 開発

packagesディレクトリ内でアプリケーションを開発します。

## 開発ルール
- biomeエラーを放置しない
- @ts-ignore や握りつぶし try-catch は禁止。
- 極力モダンな記法を採用
- 関数型プログラミング
- 深いネストは避ける
- データと関数（ロジック）は分離させる
- 単一責任の原則に従い、機能ごとにファイルを分ける
- 設定ファイルはユーザーの許可なしに変更しないこと
- ファイルはユーザーの許可なしに削除しないこと
- 機能ごとにコメントをつける（第三者目線で読んだ際、コード及びファイルの実装内容が理解できる内容にすること。開発中のユーザーとのやりとりを反映したメタ的な記述は控えること）
- スプレッドシート操作に関する関数は積極的にpackages/common/src/spreadsheet.tsをimport利用すること（spreadsheet.tsの編集はしないこと）
- １ファイルの行数は原則200行までとする。
- index.tsに直接ロジックは定義せず、関数呼び出しのみを行う純粋なGASエントリーポイントとする(各サブディレクトリ内のindex.tsも同一)
- ファイルの先頭にファイルの役割を簡潔に記載すること
- ディレクトリ構成は機能ごとに分割して、可読性を意識すること
- ファイル名は直感的に役割が伝わる名前にすること
**不明点があれば解消するまでwriteせずユーザーに質問すること**
**会話は日本語でわからないことはわからないと言うこと**

## 現在開発中プロジェクト
packages/apsisArticlePublisher
## 参考資料
packages/apsisArticlePublisher/devPlan.md
